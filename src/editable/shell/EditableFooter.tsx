'use client'
import Link from 'next/link'
import { ArrowUpRight } from 'lucide-react'
import { SITE_CONFIG } from '@/lib/site-config'
import { useEditableLocalAuthSession } from '@/editable/components/EditableLocalAuthForms'
export function EditableFooter(){const {session,logout}=useEditableLocalAuthSession();return <footer className="bg-[#0b1422] text-white"><div className="mx-auto grid max-w-[1180px] gap-10 px-5 py-14 sm:px-8 md:grid-cols-[1.3fr_1fr_1fr]"><div><Link href="/" className="text-2xl font-extrabold">{SITE_CONFIG.name}</Link><p className="mt-4 max-w-sm text-sm leading-7 text-slate-400">A focused directory for discovering local businesses, comparing services, and finding the details needed to choose confidently.</p></div><div><h3 className="text-xs font-extrabold uppercase tracking-[.22em] text-[#ffb300]">Explore</h3><div className="mt-5 grid gap-3">{[['Home','/'],['Listings','/listing'],['About','/about'],['Search','/search']].map(([l,h])=><Link key={h} href={h} className="inline-flex items-center gap-2 text-sm font-bold text-slate-300 hover:text-white">{l}<ArrowUpRight className="h-3 w-3"/></Link>)}</div></div><div><h3 className="text-xs font-extrabold uppercase tracking-[.22em] text-[#ffb300]">Account</h3><div className="mt-5 grid gap-3">{session?<><Link href="/create" className="text-sm font-bold text-slate-300">Create</Link><button onClick={logout} className="text-left text-sm font-bold text-slate-300">Logout</button></>:<><Link href="/login" className="text-sm font-bold text-slate-300">Login</Link><Link href="/signup" className="text-sm font-bold text-slate-300">Sign up</Link></>}<Link href="/contact" className="text-sm font-bold text-slate-300">Contact</Link></div></div></div><div className="border-t border-white/10 px-5 py-5 text-center text-xs text-slate-500">© {new Date().getFullYear()} {SITE_CONFIG.name}. Local businesses, clearly organized.</div></footer>}
